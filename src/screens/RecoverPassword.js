import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const RecoverPassword = () => {
  return (
    <View>
      <Text>Recovetr Pass</Text>
    </View>
  );
};

export default RecoverPassword;

const styles = StyleSheet.create({});
